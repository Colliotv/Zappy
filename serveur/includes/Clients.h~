#ifndef __CLIENT_H__
# define __CLIENT_H__

# include "bufferCirculaire.h"

typedef struct sClient Client;
struct sClient{
  /* list */
  Client*	next;
  Client*	prev;

  /* wait time */
  int		wait;

  /* Fd */
  _fd		dialogue;

  /* On Read Get Line */
  void		(*get_next_line)();
  Dialogue*	(*get_msg)();

  /* BufferCirculaire */
  BufferCirculaire*	recv;
  BufferCirculaire*	wrte;

  /* murloc info*/
  
};

#endif
